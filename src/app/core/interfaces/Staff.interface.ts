export interface PublicStaffProfile {
  userId: string;
  email: string;
  phoneNumber: string;
  name: string;
  profilePicture: string;
  lastActivityAt: Date;
  isActive: boolean;
  gender: string;
  specialization: string;
  role: number;
}
